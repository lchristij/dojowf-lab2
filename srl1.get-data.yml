---
- name: Nokia SR get data example
  host: "srl"
  gather_facts: no

vars:
  cli:
    hosts: "srl"
    username: admin
    password: NokiaSrl1!
    transport: cli

tasks:
  - name: "run show version on remote device"
    sros_command:
      commands: show version
      provider: "{{ cli }}"
        